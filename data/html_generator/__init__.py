"""HTML document and embedding generator for graph_feature_forge.

Part 2 of the data generator: reads CSVs from Part 1, generates
LLM-authored HTML documents, chunks them, and produces real embedding
vectors via the Databricks gte-large-en endpoint.
"""
